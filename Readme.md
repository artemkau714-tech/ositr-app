# 🏠 Серверный центр (Telegram Mini App)

## 🚀 Запуск
1. Залей `index.html` на GitHub Pages
2. Вставь ссылку в `WEBAPP_URL` в `bot.py`
3. Установи зависимости: `pip install -r requirements.txt`
4. Запусти бота: `python bot.py`
